services:
  - type: web
    name: elite-vinewood-auto
    runtime: node
    buildCommand: npm install --include=dev && npm exec vite build
    startCommand: npm start
    envVars:
      - key: NODE_OPTIONS
        value: --import=tsx
      - key: NPM_CONFIG_PRODUCTION
        value: "false"
