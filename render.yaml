services:
  - type: web
    name: elite-vinewood-auto
    runtime: node
    buildCommand: rm -rf dist && npm install --include=dev && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_OPTIONS
        value: --loader=tsx
