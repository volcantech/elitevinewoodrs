services:
  - type: web
    name: elite-vinewood-auto
    runtime: node
    buildCommand: rm -rf dist node_modules && npm cache clean --force && npm install --include=dev && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_OPTIONS
        value: --loader=tsx
